import React,{useState,useEffect} from 'react';
import { Link } from 'react-router-dom';

function Book(){
	return(
		<div>
		<h1>Books</h1>
		<p>This is where we implement Nodejs,Express & MongoDB to grab some data. The data below is pulled from a MongoDB database</p>

		<h2>Fetch Example<h2>
		</div>
	);
}

export default Book;
